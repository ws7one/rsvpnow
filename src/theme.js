import { colors } from 'react-native-elements';

const gold = '#FFD700';
const white = '#FFFFFF';
const black = '#000000';
const blue = '#87cefa';

const theme = {
    ...colors,
    gold,
    white,
    black,
    blue
};

export default theme;
