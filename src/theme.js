import { colors } from 'react-native-elements';

const gold = '#FFD700';
const white = '#FFFFFF';
const black = '#000000';
const blue = '#87cefa';
const darkblue = '#2d323b';

const theme = {
    ...colors,
    gold,
    white,
    black,
    blue,
    darkblue
};

export default theme;
